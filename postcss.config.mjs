/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},  // Including autoprefixer is common with Tailwind CSS
  },
};

export default config;
